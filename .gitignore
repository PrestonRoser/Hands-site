# NOTE: this file previously had unresolved merge-conflict markers committed
# into it from the Astro starter (commit 7f9bbf3). Both sides of that conflict
# have been merged into the single list below. Resolved 2026-09-12.

# build output
dist/
.output
out

# generated types / caches
.astro/
.cache/
.parcel-cache
.temp
*.tsbuildinfo
.eslintcache
.stylelintcache
.vite/
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# dependencies
node_modules/
jspm_packages/
web_modules/
bower_components

# logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json

# runtime data
pids
*.pid
*.seed
*.pid.lock

# coverage
coverage
*.lcov
.nyc_output

# environment variables
.env
.env.*
!.env.example

# package artifacts
*.tgz
.npm
.yarn-integrity
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

# deploy platforms
.vercel/
.netlify/
.serverless/

# editors / OS
.DS_Store
.idea/
.vscode-test
.node_repl_history

# ---------------------------------------------------------------------------
# Internal curriculum + standards working documents.
# These are symlinks into the Obsidian vault. They are working notes and
# procurement drafts, not site source, and must never ship in this repo.
# ---------------------------------------------------------------------------
NGSS_Alignment_Crosswalk.md
NGSS_Alignment_Gaps.md
