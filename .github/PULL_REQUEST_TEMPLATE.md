# What this changes

<!-- One or two sentences. What is different after this merges? -->

## Why

<!-- The problem being solved, not a restatement of the diff. -->

## How it was verified

<!-- Commands run, pages checked, screen widths tested. Say plainly what was NOT verified. -->

- [ ] `npm run build` passes
- [ ] `npm run check` passes
- [ ] `npm test` passes
- [ ] Checked at phone width (390px) as well as desktop

## Risk

<!-- What breaks if this is wrong, and how to undo it. -->

## Checklist

- [ ] No secrets, tokens, or `.env` contents in the diff — this repo is public
- [ ] Alt text on any new image; focus states on any new interactive element
- [ ] No unannounced product details added to the public catalog
- [ ] Branch flows `dev` → `qa` → `main` (or is a `hotfix/*`)
